# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是需要把皓月项目批注需求图转成 3D 建模参考提示词的业务人员或设计协作者。普通使用者不需要理解模型、提示词结构或 API 参数，只需要上传一张完整需求图，并可选补充一句文字要求。部署者可在同一页面进入独立“服务配置”工位，填写解析模型连接和 PixPark Token。

## Product Purpose

把“上传需求图—解析批注—生成并审查提示词—人工确认—生成结果图”压缩为一条可控流程。成功意味着第一次使用的用户无需培训，也能先检查可直接复制的提示词，再自行决定是否调用生图服务。

## Positioning

产品不是通用 AI 对话框，而是把 `haoyue-3d-concept` Skill 的双图职责、布局权限、品牌规则、灰色建模背景和提示词审查固化成自动执行流水线。

## Operating Context

- 普通用户上传图1需求图，并可选填写补充说明。
- 部署者可通过页面配置模型 API、模型名称、模型 API Key、PixPark Token 和是否允许结果图生成；也可继续使用 `.env`。
- 固定的图2人工建模风格参考图仍由部署者放入服务器目录，不允许普通任务上传替换。
- 后台自动识别任务类型、加载适用规则、调用视觉模型并运行机械校验。
- 文本审查完成后流程停在“待确认”，普通用户确认提示词无误后才进入 PixPark。
- 同一任务允许按返工对象分流：创意或提示词不满意时重新走完整解析和审查；仅结果图不满意时保留提示词，重新生成一组四图。
- 结果完成后允许建立修改版本：补充要求重新开放编辑，图1可沿用或替换；修改版使用新任务编号，旧任务与旧结果不被覆盖。
- 解析或生图进入后台后，用户可以立即另起一项任务；历史轨道负责切换查看、补充、取消和验收各自独立的任务。

## Capabilities and Constraints

- 不包含用户账号或面向普通任务的模型参数面板；页面只提供部署级服务连接配置。
- 同页提供 TTL 临时保留期内的任务历史，可回看需求图、提示词和结果图；浏览器刷新或服务重启后仍可恢复，到期后自动清理。
- 桌面端采用 16:9 单屏工作台，主要流程不依赖页面纵向滚动。
- 支持 Docker 容器部署和 OpenAI 兼容的多模态模型接口。
- API Key 与 PixPark Token 只保存在服务端私有运行目录或环境变量中；浏览器读取时只获得“已配置/未配置”。
- 图1只在服务器私有运行目录中按任务临时保存，到期或取消时与任务元数据一起清理，不作为永久素材库。
- 图2是部署级固定配置，普通用户不可更改。
- Skill 完成提示词审查后先交付正向提示词；只有普通用户点击“确认提示词，开始生图”且部署配置允许时，才调用 PixPark。
- 正向提示词、负面提示词、解析详情和结果图在同一结果工位内切换；正向提示词是默认首屏。
- “修改需求”进入输入工位内的明确修订态，显示来源任务短编号、原图沿用状态和取消入口；没有重新选图时也可仅提交文字修改。

## Brand Commitments

产品名称暂用“皓月 3D 需求解析”。界面语言为简洁、直接的中文，优先使用“上传需求图”“补充要求”“开始解析”“复制提示词”等任务语言，不使用模型术语教育用户。

## Evidence on Hand

- `SKILL.md`：产品规则与工作流的事实来源。
- `references/`：批注解析、布局、品牌、材质、提示词和审查规则。
- `scripts/validate_output.py`：文本交付的机械门禁。
- `agents/openai.yaml`：现有 Skill 名称和默认触发描述。
- 当前经确认的固定图2正式资产位于 `config/style-reference.png`，普通任务不可替换。

## Product Principles

1. 从核心任务倒推界面，普通用户只面对完成任务所需的最少选择。
2. 自动化复杂度留在后台，结果和失败原因在前台清楚可见。
3. 先保证提示词可追踪、可校验、可人工确认，再追求速度和视觉表现。
4. 文本结果与生成图属于同一个任务对象，避免重复上传和重新配置。
5. 不虚构模型能力、生成结果、客户案例或处理时长。
6. 当前查看对象与后台执行队列必须解耦，一个慢任务不能锁住整个上传工位。

## Accessibility & Inclusion

上传、输入、提交、复制和结果展开均需支持键盘操作、可见焦点、清晰状态文案和移动端使用；不得只依赖颜色表达成功或失败。
