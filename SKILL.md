---
name: haoyue-3d-concept
description: 将皓月3D项目的一张完整批注需求图转换为可直接粘贴的中文3D建模参考生图提示词，并执行需求编号、布局权限、灰色建模背景、可建结构和图文对照审查。生图页面固定使用图1需求图与图2既有风格参考图，不搜索或引用第三张图，默认不生图。用户明确调用本 Skill，或提到皓月项目、皓月螺丝项目、蓝色螺丝小人项目的需求图出提示词、3D小景或建模参考时使用；其他通用3D提示词任务不要隐式触发。
---

# 皓月需求图 → 3D 建模参考提示词

## 目标

面向 3D 建模师，把一张含主画面、批注、箭头、红线和角落局部参考的完整需求图，转换为：

1. 可追踪的需求终态；
2. 可直接提交给生图模型的纯净中文提示词；
3. 对照原图完成的提示词审查；
4. 可拆、可承托、可固定的建模要点。

以“建模师能否照图建”为最终标准：轮廓和比例可量、连接和承托可读、材质分区清楚、关键结构无遮挡。默认只输出文本，禁止调用 ImageGen、Nano Banana 或其他生图工具。

## 固定输入契约

- 用户在对话中只需上传一张完整需求图，固定记为**图1**。
- 生图页面固定只有两张输入：图1需求图、图2既有风格参考图。
- 图1决定内容、身份、结构、配色、文字终态、批注、局部参考和成熟布局。
- 图2只提供人工建模质感：体块、分件、材质表现、圆角、细节等级和灯光结构；不得提供人物、动物、物件、文字、Logo、颜色、布局、环境、背景、镜头或构图。
- 图1里的角落裁切小图仍属于图1内部，绝不编号为图3。
- 不搜索、不匹配、不增加其他参考图；最终提示词只能引用图1和图2。

触发后先简短说明将自动解析、审查且不生图，然后直接工作。只有批注无法可靠识别、要求互相冲突、出现“找我方要”的待提供内容，或有来源但可能过期的时效数字时，才问一个聚焦问题。

## 唯一优先级

| 层级 | 内容 |
|---|---|
| L0 | 固定双图契约、当前任务隔离、中性灰背景、批注文字不入画、纯净复制区、不确定先问、图文对照通过后交付、默认不生图 |
| L1 | 用户本轮文字、批注、箭头、红线/红叉/红圈所表达的硬修改 |
| L2 | 图1可见事实；成熟主画面的镜头与模型构件布局也属于事实 |
| L3 | 皓月品牌规范、第三方 IP 预警、默认无意外文字 |
| L4 | 图2允许提供的建模风格与项目默认结构语言 |
| L5 | 当前任务类型授权的创意 |

低层无条件让位高层；同层以更具体的要求为准。客户已决定的内容只执行和校验，未决定区域才允许创作。

## 全任务硬规则

### 当前任务隔离

每轮从当前图1和用户本轮文字重新建立允许内容清单。提示词中的每个角色、动物、物件、场景模块、动作、文字和周年元素都必须能回指当前来源或本轮明确授权的创意；禁止沿用上一任务内容。

### `PROJECT-BG`

每单自动登记背景硬要求：

- 外部背景为单一中性灰色、无纹理、无图案、无场景、无地平线的无缝建模展示背景，只允许柔和影棚光和极轻接触阴影。
- 仅按主体轮廓对比在浅灰、中灰、深灰间选择；不得使用白、黑、米、棕、主题色或彩色渐变背景。
- 删除图1的天空、街景、建筑、房间、远山、无边界草地及其他环境内容；环境氛围只转译为主体灯光色温。
- 与主体物理连接、需要建模且边界清楚的底座、书页、背板、立牌、印刷画板和实体特效件属于模型构件，继续保留。
- 能直接承托多个角色或道具的有边界草地、泥土、木台、岩层、水池边框或铺装是**有限模型底座**，不是环境。

### 连续成图与可建性

- 最终画面是一张连续完整的 3D 渲染，不得复制图1中的参考截图、裁切块、白边/黑边、分屏、画中画、说明卡或拼贴版式。
- 每个构件有厚度、接触、承托、固定或插接依据；水花、泡泡、烟花、音符、气球、彩纸和蒸汽等效果件必须说明实体材质和固定点。
- 用户未要求显示画幅比例时，不写“生成1:1”等可能被画进画面的标志。

## 标准流程

### 1. 解析图1

完整读取 `references/annotation-parsing.md`，重新查看图1并完成：

- OCR 区分需求批注、物体原文、局部参考和噪音；
- 把图1中的手写数字建立为内部序号绑定，逐项记录指向区域、目标对象、允许继承、禁止继承和最终实例数；
- 沿箭头检查文字、箭身、两端、相邻裁切小图和最终目标；
- 用户把红线定义为删除时，建立逐红线目标台账，不得只处理最显眼的一处；
- 把复合批注拆成独立 `R` 编号；
- 为替换、删除、数量、文字和空间关系建立可观察终态；
- 把“可加、可以增加、必要时添加”登记为 `CONDITIONAL_ADD`，结合丰富开关与布局权限明确执行或不执行；
- 登记 `PROJECT-BG`、允许内容清单和待确认项。

### 2. 判定任务类型

| `TASK_TYPE` | 适用 | 权限 |
|---|---|---|
| `RECOMPOSE_SCENE` | 素材照、2D概念、散乱拼贴、粗稿，或用户明确要求重设计 | 保真识别特征，允许重组为紧凑叙事小景 |
| `LOCK_LAYOUT_EDIT` | 完整效果图、成熟构图、用户说布局OK或只改局部 | 冻结镜头及全部模型构件的数量、位置、比例、朝向、间距和遮挡 |
| `ASSET` | 单体角色、机械、道具或产品 | 保持轮廓和功能，允许必要结构补全与克制摆拍 |
| `STRICT_REPLICA` | 严格复刻、只转材质、只换角色或只改指定部位 | 未点名内容全部冻结，禁止自由新增和重构 |

多种类型同时命中时选限制最严格者；只有用户明确授权重设计时才选 `RECOMPOSE_SCENE`。

按类型读取：

- `LOCK_LAYOUT_EDIT` 或 `STRICT_REPLICA`：完整读取 `references/layout-lock.md`。
- `RECOMPOSE_SCENE`：完整读取 `references/recompose-scene.md`；仅需要案例启发时再读 `references/creative-cases.md` 中最接近的 1～2 个案例，完整范例见 `references/example-durian.md`。
- `ASSET`：不读取创意案例；只执行本文件与风格、材质、审查规则。

### 3. 按需求读取专项规则

- 出现纸板、纸艺、毛绒、金属、玻璃、软胶或其他特殊材质：读取 `references/style-and-materials.md`。
- 出现文字、字母、数字、周年、年份、期数、Logo、徽章或同类标志替换：读取 `references/text-logo-time.md`。
- 出现皓月蓝色螺丝小人、螺丝Logo或职业服装：读取 `references/brand-haoyue.md`，必要时只查看其中指定的两张 IP 图片。

没有命中条件时不要读取对应文件。

### 4. 建立状态变化卡

每个 `R` 项必须包含：

| 字段 | 内容 |
|---|---|
| 来源 | 用户文字、批注、箭头、局部参考或图1可见事实 |
| 内部序号绑定 | 对应的“图1内部手写序号N”、指向证据和目标；没有时写 `N/A` |
| 允许继承 | 该局部参考可提供的身份、造型、颜色、姿态、材质或数量 |
| 禁止继承 | 不得带入的颜色、造型、角色、道具、文字、背景和额外实例 |
| 目标集合 | 受影响的全部实例；职责不同的同类对象先分组 |
| 操作 | `KEEP`、`REMOVE`、`REPLACE`、`ADD` 或局部变形 |
| 最终状态 | 可直接验收的数量、字符串、方向、位置、连接或缺席状态 |
| 冻结项 | 镜头、载体、布局、其他实例和功能中不能变化的部分 |
| 影响范围 | 变化允许发生的部件、占位或局部模型轴 |
| 功能继承 | 原功能保持、删除或替换；未点名时自动保持 |

任一字段不完整不得进入提示词。审查的是可观察终态，不是“是否提到过需求”。

### 5. 应用丰富趣味开关

网站会随本轮任务传入 `ENRICHMENT_ENABLED=true/false`。无论开关状态如何，都必须先完成图1解析、任务类型判断、状态变化卡、布局权限和全部硬要求终态；开关不得影响 OCR、需求分类、双图职责或冻结边界。

- `false`：执行本 Skill 原有完整流程和当前任务类型允许的正常创意，不额外提高功能模块、叙事细节或结构复杂度。
- `true`：完整读取 `references/enrichment.md`，只在硬要求和布局权限允许的区域增加可建模的主概念、功能模块、因果细节、重复结构与空间厚度；不得把开关理解为自由改布局或堆放小道具。
- 图1已有安全的附着式 `CONDITIONAL_ADD` 时，`true` 优先把该项收敛为准确数量、位置、连接和不扩散范围；`false` 默认不触发但仍在需求解析表中保留。

丰富结果必须写入“创意方向”、纯净正向提示词和图文对照审查。任一丰富内容无法说明来源权限、功能、位置、承托、固定或拆件方式时，删除该内容，不得牺牲硬要求换取丰富度。

### 6. 组装交付

完整读取 `references/prompt-contract.md`，先写硬修改摘要，再写布局、主体、材质、结构、创意和渲染。纯净提示词必须：

- 只引用图1和图2；
- 图1存在手写序号时，使用“图1内部手写序号N”的字面句式绑定职责，禁止把内部序号改写成图N；
- 前置图2越权排除和批注文字不入画；
- 在提示词前半部分集中写清唯一主画面、内部序号职责、逐红线删除、替换数量和条件新增终态；
- 用自然语言表达任务权限，不泄露内部变量；
- 正向写明中性灰建模背景、有限底座和连续成图；
- 把删除与替换写成唯一终态，不依赖负面词补救。

### 7. 审查与重写

完整读取 `references/prompt-audit.md`。完成提示词后必须重新打开当前图1，先总览、再九宫格逐区对照，执行：

1. `R` 编号到提示词句子的逐项追踪；
2. 状态卡完整性检查；
3. 内部序号绑定表与逐红线台账检查；
4. 模型捷径与提示词稀释测试；
5. 生图模型能否仅凭正向提示词回答主画面、参考职责、删除、替换、数量和条件新增；
6. 图1可见证据与提示词终态对照；
7. 当前任务、双图、背景、布局、材质、文字和可建结构门禁。

任一适用项失败都内部重写，并从图文对照重新开始；只交付 `PROMPT_AUDIT=PASS` 的版本。若本地可执行脚本，再运行：

```bash
python3 scripts/validate_output.py <draft-output.md>
```

脚本只负责图号、复制区污染、占位符、背景硬句等机械门禁，不能替代视觉和语义审查。

## 输出格式

按以下顺序输出；不适用内容写 `N/A`，不要为使审查项适用而新增画面元素。

1. `识别到的需求文字`
2. `需求解析表`：`R` 编号、内部序号绑定、允许继承、禁止继承、目标集合、操作、终态、冻结边界，并固定加入 `PROJECT-BG`
3. `双图职责与风格策略`
4. `任务类型与布局权限`
5. `创意方向`：重构任务批注明确时给1个方向，模糊时给2～4个；成熟改稿仅原位互动；资产和严格复刻可为 `N/A`。勾选丰富趣味时按 `references/enrichment.md` 中对应任务类型的权限上限补充主概念、功能模块、因果细节、重复结构和建模减法；锁布局与严格复刻不得为了凑数量越权新增。
6. `纯净生图提示词复制区`
7. `纯净负面提示词复制区`
8. `提示词审查`：需求追踪、内部序号绑定、逐红线台账、精简九宫格证据、生图指令清晰度和适用硬门禁
9. `自检报告`：重写次数、残余风险、待确认项；不打总分
10. `建模要点清单`：拆件、背面、固定、材质分区和数字规格；无则省略

审查可以在内部完整执行，但用户可见区只展开适用项和证据，不重复罗列大量 `N/A`。

## 临时结果诊断

正式流程在上述交付后结束，不主动要求回传结果。只有用户明确上传生成结果并要求“对比、检查、分析原因”时，才读取 `references/result-review.md`；该资料只服务当轮诊断，不成为第三张输入，不写入下一单。

## 项目资源边界

- `整理/`、`需求/`、`人工效果/`、`新的需求.md` 和 `tests/` 是开发与回归资料，正式运行不得读取，也不得作为生图输入。
- 打包时只保留 `SKILL.md`、`agents/`、`references/` 和 `scripts/`；不包含开发资料与测试。
- 两张螺丝 IP 图片只在 `brand-haoyue.md` 指定条件下查看，不作为场景、布局或通用风格参考。
